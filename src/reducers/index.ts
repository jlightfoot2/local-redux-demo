import {makeProduct} from '../res/data/products'

const products = (state,action) => {
  switch (action.type) {
    case "value":
      // code...
      break;
    
    default:
      // code...
      break;
  }
  return state;
}


export default products;

